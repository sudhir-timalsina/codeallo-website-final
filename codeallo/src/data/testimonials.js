// Intentionally empty. Codeallo doesn't have collected, attributable client
// testimonials yet, and inventing names or quotes would be worse than
// having none. Add real entries here (or manage them via the `testimonials`
// table + admin dashboard once that's in use) as they're actually
// collected. The UI renders a proper empty state instead of fake content —
// see components/sections/TestimonialsSection.jsx.

export const testimonials = []
